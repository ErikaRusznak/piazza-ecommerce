{
  "name": "frontend",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/config/*",
    "packages/shared/*"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.5.2",
  "dependencies": {
    "turborepo": "^0.0.1"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "turbo run format"
  }
}
